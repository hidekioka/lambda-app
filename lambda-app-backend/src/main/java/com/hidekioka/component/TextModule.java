package com.hidekioka.component;

import dagger.Module;

@Module
class TextModule {
	// Module is used to inject dependencies when just @Inject isn't available,
	// isn't necessary for this project
}
